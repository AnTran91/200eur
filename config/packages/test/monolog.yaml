monolog:
    handlers:
        main:
            level:    debug
            type:     rotating_file
            path:     "%kernel.logs_dir%/%kernel.environment%.log"
            channels: ["!event"]
