<form method="post" action="{{ path('admin_picture_retouch_delete', {'id': picture_retouch.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture_retouch.id) }}">
    <button class="btn">Delete</button>
</form>
