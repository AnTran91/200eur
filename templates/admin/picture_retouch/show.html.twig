{% extends 'admin/base.html.twig' %}

{% block title %}PictureDetails{% endblock %}

{% block body %}
    <h1>PictureDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picture_retouch.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ picture_retouch.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_picture_retouch_index') }}">back to list</a>

    <a href="{{ path('admin_picture_retouch_edit', {'id': picture_retouch.id}) }}">edit</a>

    {{ include('admin/picture_retouch/_delete_form.html.twig') }}
{% endblock %}
